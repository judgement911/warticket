{
  "_readme": "PRIMARY: NCT 127 5TH TOUR 'NEO CITY : JAKARTA - THE REDLINE', Indonesia Arena, 3 Oct 2026. General sale Fri 21 Aug 2026 14:00 WIB, max 4 tickets per transaction. Watched on both the -03 event mirror and the canonical dyandraglobalstore.com, because which host opens the buy button could not be verified from the build environment. NOT sold on loket.com. The Maroon 5 targets are left running. The monitor alerts with the real checkout link; checkout.py fills the cart and stops before payment.",
  "heartbeat_hours": 12,
  "targets": [
    {
      "name": "🔥 NCT 127 REDLINE — event page",
      "_note": "NCT 127 5TH TOUR 'NEO CITY : JAKARTA - THE REDLINE', 3 Oct 2026, Indonesia Arena. GENERAL SALE Fri 21 Aug 2026 14:00 WIB — the NCTzen and Dyandra presales are already gone. Max 4 tickets per transaction, one account. This -03 host is the event page itself: two search indexes show it serving the tour title, which is what finally corroborated the domain. Payment is bank VA, bank transfer or credit card — all of it yours to do by hand.",
      "enabled": true,
      "url": "https://dyandraglobalstore-03.com/",
      "rules": [
        {
          "_note": "Fires when a real purchase path appears — /checkout, /cart, a midtrans/xendit gateway URL, or a new /event/ page. Static assets are excluded, so a redeploy that renames checkout-<hash>.js does not read as the sale opening.",
          "type": "new_link",
          "pattern": "(/checkout|/cart|/order|/payment|/purchase|/buy|/booking|/queue|/event/|/ticket|midtrans|xendit|doku|faspay|queue-it\\.net)",
          "fire_on_first": false
        },
        {
          "_note": "Safety net: a 'coming soon' block turning into a product card, or any copy change the link rule would miss.",
          "type": "changed",
          "ignore_pattern": "(csrf[^\"]*\"[^\"]*\"|_token[^\"]*\"[^\"]*\"|nonce=[^\"&]*|buildId[^,]*|\\?v=\\d+|\\d{13})"
        }
      ],
      "interval": 20,
      "hot_interval": 2,
      "hot_window_min": 60,
      "cooldown": 300,
      "open_url": "https://dyandraglobalstore-03.com/",
      "alert_repeat": 12,
      "alert_repeat_every": 25,
      "launch_checkout": false,
      "_checkout_note": "Set true once checkout.steps is filled in and tested. The monitor then spawns checkout.py against the detected link the moment the sale opens, so the clicking is already done when you pick up the phone.",
      "drop_time": "2026-08-21T14:00:00"
    },
    {
      "name": "🔥 NCT 127 REDLINE — canonical store",
      "_note": "ON despite the event-page-only choice, deliberately. Press coverage says the general sale runs on dyandraglobalstore.com while the tour page lives on the -03 mirror, and nothing here could reach either to settle which host actually grows the buy button. Watching one and guessing wrong costs the ticket; watching both costs one extra request every 30s. Disable if you confirm the sale is -03 only.",
      "enabled": true,
      "url": "https://dyandraglobalstore.com/",
      "rules": [
        {
          "_note": "Same purchase-path detection as the mirror.",
          "type": "new_link",
          "pattern": "(/checkout|/cart|/order|/payment|/purchase|/buy|/booking|/queue|/event/|/ticket|midtrans|xendit|doku|faspay|queue-it\\.net)",
          "fire_on_first": false
        },
        {
          "_note": "Safety net: a 'coming soon' block turning into a product card, or any copy change the link rule would miss.",
          "type": "changed",
          "ignore_pattern": "(csrf[^\"]*\"[^\"]*\"|_token[^\"]*\"[^\"]*\"|nonce=[^\"&]*|buildId[^,]*|\\?v=\\d+|\\d{13})"
        }
      ],
      "interval": 30,
      "hot_interval": 3,
      "hot_window_min": 60,
      "cooldown": 300,
      "open_url": "https://dyandraglobalstore.com/",
      "alert_repeat": 12,
      "alert_repeat_every": 25,
      "drop_time": "2026-08-21T14:00:00"
    },
    {
      "name": "DYANDRA STORE -02 — spare mirror",
      "_note": "Off. -03 is this event's mirror, so -02 is most likely a previous sale. Nothing to watch here unless Dyandra reshuffles hosts.",
      "enabled": false,
      "url": "https://dyandraglobalstore-02.com/",
      "rules": [
        {
          "_note": "Same purchase-path detection as the mirror.",
          "type": "new_link",
          "pattern": "(/checkout|/cart|/order|/payment|/purchase|/buy|/booking|/queue|/event/|/ticket|midtrans|xendit|doku|faspay|queue-it\\.net)",
          "fire_on_first": false
        },
        {
          "_note": "Safety net: a 'coming soon' block turning into a product card, or any copy change the link rule would miss.",
          "type": "changed",
          "ignore_pattern": "(csrf[^\"]*\"[^\"]*\"|_token[^\"]*\"[^\"]*\"|nonce=[^\"&]*|buildId[^,]*|\\?v=\\d+|\\d{13})"
        }
      ],
      "interval": 30,
      "hot_interval": 3,
      "cooldown": 300,
      "open_url": "https://dyandraglobalstore-02.com/"
    },
    {
      "name": "🔥 MAROON 5 — official sale site",
      "_note": "PRIMARY. Right now the Artist Presale / BCA Presale / General On-Sale buttons are dead links (href=#). When a window opens they become real tiket.com queue links — that is the change this catches. Server-rendered, so the bot sees the real HTML.",
      "enabled": true,
      "url": "https://maroon5jakarta2027.com/",
      "rules": [
        {
          "_note": "THE one that matters: a dead href=# button becoming a real tiket.com/queue URL. Alerts with the actual link, bypasses cooldown, and nags until you /ack. First poll only takes a baseline so existing footer links don't false-fire.",
          "type": "new_link",
          "pattern": "(tiket\\.com|loket\\.com|queue-it\\.net|/checkout|/queue|/order|/booking)",
          "fire_on_first": false
        },
        {
          "_note": "Safety net: catches a redesign or a copy change that the link detector would miss.",
          "type": "changed",
          "ignore_pattern": "(csrf[^\"]*\"[^\"]*\"|_token[^\"]*\"[^\"]*\"|nonce=[^\"&]*|\\?v=\\d+|\\d{2}:\\d{2}:\\d{2})"
        }
      ],
      "interval": 20,
      "hot_interval": 1.5,
      "drop_time": "2026-08-29T10:00:00",
      "hot_window_min": 60,
      "cooldown": 300,
      "open_url": "https://maroon5jakarta2027.com/",
      "alert_repeat": 12,
      "alert_repeat_every": 25
    },
    {
      "name": "MAROON 5 — tiket.com backup",
      "_note": "DISABLED — tiket.com returns 403 to datacenter IPs, and the page is JavaScript-rendered so the bot could never see button text anyway. The official sale site target is the real one. Leave this off.",
      "enabled": false,
      "url": "https://www.tiket.com/id-id/explore/maroon5asiajakarta",
      "rule": {
        "type": "changed",
        "ignore_pattern": "(csrf[^\"]*\"[^\"]*\"|_token[^\"]*\"[^\"]*\"|nonce=[^\"&]*|buildId[^,]*|\\d{13})"
      },
      "interval": 45,
      "hot_interval": 3,
      "drop_time": "2026-08-29T10:00:00",
      "hot_window_min": 60,
      "cooldown": 900
    },
    {
      "name": "MAROON 5 — S.I.N. fan club signup",
      "_note": "Artist Presale on 26 Aug needs a code from this fan club. Watches the signup page in case tiers or availability change. Off by default — enable only if you join.",
      "enabled": false,
      "url": "https://maroon5sin.com/subscribe",
      "rule": {
        "type": "changed"
      },
      "interval": 300,
      "cooldown": 3600
    }
  ],
  "checkout": {
    "_note": "Steps for checkout.py, in QUEUE-FIRST order: get into the waiting room before anything else, hold the slot, and only then type. Nothing is asked of you at run time — profile.json is already on disk, so entry is never waiting on data entry. The map is selector-free on purpose so it works with no advance knowledge of the store's DOM: enter_queue finds the buy control by its wording, and fill_profile matches fields by name/id/placeholder/autocomplete. To pin the exact selectors, run `python3 checkout.py inspect <url>` against the LIVE sale page. Actions: enter_queue, wait_for_queue, click, select, fill, fill_profile, wait_for, goto, screenshot, sleep. Mark early steps \"optional\": true — the monitor hands over whatever URL it detected, which may already be past them. fill_profile refuses to run while a waiting room is on screen, and the driver halts before anything that looks like paying, whatever these steps say.",
    "_example": [
      {
        "do": "enter_queue",
        "selector": "#buy-button",
        "note": "pinned selector from `inspect`, faster than text matching"
      },
      {
        "do": "wait_for_queue",
        "admitted_text": "Pilih Kategori",
        "note": "explicit admission marker beats the field-count heuristic"
      },
      {
        "do": "click",
        "text": "CAT 1",
        "note": "tier",
        "optional": true
      },
      {
        "do": "select",
        "selector": "#qty",
        "value": "2"
      },
      {
        "do": "fill_profile"
      },
      {
        "do": "click",
        "text": "LANJUTKAN",
        "note": "halts at payment"
      }
    ],
    "step_timeout": 15,
    "steps": [
      {
        "do": "enter_queue",
        "note": "FIRST ACTION: click into the waiting room. Retries for 90s because the button is often disabled for the first second or two of a sale.",
        "window": 90,
        "retry_every": 0.35
      },
      {
        "do": "wait_for_queue",
        "note": "Hold the slot until admitted. Never reloads — a refresh is how you give the place back. 45 min of patience.",
        "timeout": 2700,
        "poll_every": 2
      },
      {
        "do": "wait_for",
        "selector": "input, textarea, select",
        "note": "buyer form on screen",
        "optional": true,
        "timeout": 60
      },
      {
        "do": "fill_profile",
        "note": "name, email, phone, KTP — only ever after admission"
      },
      {
        "do": "screenshot",
        "note": "filled-form"
      }
    ]
  }
}
